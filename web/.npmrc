# Railway builds `npm ci` with the Node 22 image's npm 10, which refuses the
# lockfile's peer graph (@coinbase/cdp-sdk via @base-org/account) that npm 11
# accepts locally. Resolve peers the legacy way so the lockfile installs as-is.
legacy-peer-deps=true
